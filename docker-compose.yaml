version: '3.8'

services:
  app:
    build: .
    container_name: finance-app
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - ./keys:/app/keys